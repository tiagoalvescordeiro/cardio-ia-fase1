# Node-RED — fluxos exportados

1. No Node-RED: **Menu** → **Exportar** → salve o JSON nesta pasta (ex.: `dashboard_flow.json`).  
2. No README principal, referencie o arquivo e descreva tópicos MQTT e limites de alerta.
